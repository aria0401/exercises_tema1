"use strict";

const name = "ariadna";
const firstString = name.substring(0, 1);
const upperLetter = name.substring(1, 3);
const secondString = name.substring(3);
const theUpperletter = upperLetter.toUpperCase();

console.log(`${firstString}${theUpperletter}${secondString}`);
